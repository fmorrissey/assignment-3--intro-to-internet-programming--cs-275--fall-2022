window.onload() =>
{
    let body = document.querySelector(body);
    let script = document.createElement(script);
        
        script.setAttribute(scr,json/data.json);
        body.append(script);
}
